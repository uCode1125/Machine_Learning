import pyautogui as pag
import keyboard
pos_list=[]
while True:   
    while True:
        if keyboard.is_pressed('F3'): 
            pos = pag.position()
            pos_list.append(pos)
            print(pos)
            break
    while True:
        if keyboard.is_pressed('F4'): 
            break